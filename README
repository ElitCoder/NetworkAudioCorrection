Build essentials:
apt: ./install_apt.sh
pacman: ./install_pacman.sh

FIRST USE (Server & Client):
1. Install build essentials using the command above
2. Compile using ./create.sh
3. Start Server by cd Server/ && ./Server and start client by cd Client/ && ./Client

Configuration:
Server has a config file at Server/config
Client has a config file at Client/config

Guide:
The Server controls all connections to the hardware devices while the Client requests the Server.